# پلاگین خرید درون‌برنامه‌ای فلاتر (Flutter) مایکت

در این مستند با نحوه‌ پیاده‌سازی سرویس پرداخت درون‌برنامه‌ای مایکت برای برنامه‌های تحت Flutter آشنا می‌شوید.

## نصب Plugin
جهت پیاده‌سازی In App Billing در برنامه‌های Flutter کافی ‌است dependency مایکت را به `pubspec.yaml` خود به صورت زیر اضافه کنید:

```yaml
dependencies:
  myket_iap: ^1.1.10
```

سپس پکیج را در کلاس‌های مورد نظر Import کنید:

```dart
import 'package:package:myket_iap/myket_iap.dart';
```

## تنظیمات اندروید (AndroidManifest.xml)
در نسخه‌های جدید، نیازی به اضافه کردن دستی دسترسی `BILLING` یا `queries` در فایل `AndroidManifest.xml` نیست و این تنظیمات به صورت خودکار توسط پکیج مدیریت می‌شوند. اگر از نسخه‌های قدیمی (قبل از ۱.۱.۰) استفاده می‌کردید، حتماً موارد مربوط به مایکت را از مانیفست حذف کنید.

## تنظیمات Gradle (build.gradle)
در فایل `android/app/build.gradle` مقادیر زیر را در بخش `defaultConfig` اضافه کنید:

```gradle
android {
    defaultConfig {
        // ...
        def marketApplicationId = "ir.mservices.market"
        def marketBindAddress = "ir.mservices.market.InAppBillingService.BIND"

        manifestPlaceholders += [
            marketApplicationId: "${marketApplicationId}",
            marketBindAddress : "${marketBindAddress}",
            marketPermission : "${marketApplicationId}.BILLING"
        ]
    }
}
```

## پیاده‌سازی فنی

### ۱. مقداردهی اولیه (Initialize)
در متد `initState` برنامه، سرویس پرداخت را با کلید RSA خود (که از پنل توسعه‌دهندگان مایکت دریافت کرده‌اید) فعال کنید:

```dart
var iabResult = await MyketIAP.init(
  rsaKey: "YOUR_RSA_PUBLIC_KEY",
  enableDebugLogging: true,
);
```

### ۲. شروع فرآیند خرید (Purchase Flow)
برای خرید یک محصول (مصرف‌شدنی، مصرف‌نشدنی یا اشتراک) از متد `launchPurchaseFlow` استفاده کنید:

```dart
Map<String, dynamic> result = await MyketIAP.launchPurchaseFlow(
  sku: "product_id", 
  payload: "optional_developer_payload",
);

IabResult purchaseResult = result[MyketIAP.RESULT];
Purchase purchase = result[MyketIAP.PURCHASE];
```

**نکته برای اشتراک:** برای محصولات اشتراکی نیز از همین متد با شناسه محصول (SKU) مربوطه استفاده می‌شود.

### ۳. مصرف محصول (Consume)
برای محصولاتی که از نوع «مصرف‌شدنی» هستند، باید پس از اتمام خرید و تحویل کالا به کاربر، محصول را مصرف کنید تا کاربر بتواند مجدداً آن را خریداری کند:

```dart
Map<String, dynamic> result = await MyketIAP.consume(purchase: purchase);
```

### ۴. بررسی وضعیت خرید (Query Purchases)
برای گرفتن وضعیت یک محصول خاص:

```dart
Map<String, dynamic> result = await MyketIAP.getPurchase(
  sku: "product_id", 
  querySkuDetails: true,
);
```

و برای دریافت لیست تمام محصولات خریداری شده توسط کاربر (از جمله اشتراک‌های فعال):

```dart
Map<String, dynamic> result = await MyketIAP.queryInventory(querySkuDetails: true);

Inventory inventory = result[MyketIAP.INVENTORY];
```

### ۵. آزادسازی منابع (Dispose)
در پایان برای آزادسازی منابع:

```dart
await MyketIAP.dispose();
```

---
**توجه:** تمامی ارجاعات به سایر مارکت‌ها از این مستند حذف شده و این راهنما به صورت اختصاصی برای سرویس پرداخت درون‌برنامه‌ای مایکت تهیه شده است.
